from setuptools import setup, find_packages

setup(
    name="vjs-py",
    version="0.0.1",
    description="Types for vjs-frontend, python-backend",
    packages=find_packages(),
    python_requires=">=3.10",
    install_requires=[],
)